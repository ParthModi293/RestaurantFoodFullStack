package com.food.RestaurantRepository;

import com.food.RestaurantEntity.ManageOffers;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ManageOffersRepository extends JpaRepository<ManageOffers,Long> {
}
